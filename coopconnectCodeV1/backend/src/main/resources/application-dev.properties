# Development Configuration — complementary to application.yml dev profile
# This file is loaded when spring.profiles.active=dev (set in application.yml)

# Email Configuration (disabled for development)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.test-connection=false
